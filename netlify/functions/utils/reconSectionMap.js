// Server-side mirror of src/utils/reconSectionMap.js — keep in sync.
// Netlify functions cannot import from src/ so constants are duplicated here.

export const MESSAGING_SECTION_ID = 9;
export const ICP_LEVEL_SECTIONS = [9];
export const DEFAULT_ICP_ID = 'default';
