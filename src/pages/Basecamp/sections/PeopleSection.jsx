/**
 * PeopleSection — People view in Basecamp.
 * Renders AllLeads with mode='basecamp' for customer contact management.
 */
import AllLeads from '../../Scout/AllLeads';

export default function PeopleSection() {
  return <AllLeads mode="basecamp" />;
}
